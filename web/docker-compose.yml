services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: twosteps
      POSTGRES_PASSWORD: twosteps
      POSTGRES_DB: twosteps
    ports:
      - "5433:5432"
    volumes:
      - twosteps_pg:/var/lib/postgresql/data

volumes:
  twosteps_pg:
