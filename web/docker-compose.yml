services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: pushkinson
      POSTGRES_PASSWORD: pushkinson
      POSTGRES_DB: pushkinson
    ports:
      - "5433:5432"
    volumes:
      - pushkinson_pg:/var/lib/postgresql/data

volumes:
  pushkinson_pg:
